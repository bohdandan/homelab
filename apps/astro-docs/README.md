# Astro Docs

Minimal Starlight source tree for the docs site.

- `npm run dev` for local development
- `npm run build` to produce the static site in `dist/`
- `npm run preview` to preview the built output
