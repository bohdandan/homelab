# Copy this file to .env and fill in your values

# Proxmox connection
PROXMOX_HOST=
PROXMOX_NODE=pve

# Proxmox API Token (separate ID and secret)
# Format: username@realm!tokenname
PROXMOX_API_TOKEN_ID=terraform@pve!opentofu
# The secret value (UUID format)
PROXMOX_API_TOKEN_SECRET=
